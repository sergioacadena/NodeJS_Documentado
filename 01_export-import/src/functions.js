

export const getTitle = () => 'curso de NodeJS'; // Función que devuelve el título del curso anónima arrow 
// const getTitle = function() { return 'curso de NodeJS'; } // Función que devuelve el título del curso anónima

export const getAuthor = (author) => `el Autor del curso es ${author}`; // Función que devuelve el autor del curso anónima arrow
// const getAuthor = function(author) { return `el Autor del curso es ${author}`; } // Función que devuelve el autor del curso anónima


/* EXPORTACIÓN VERSIÓN ANTIGUA */
/* module.exports = { // Exporta las funciones getTitle y getAuthor para que puedan ser utilizadas en otros archivos 
    getTitle, // Exporta la función getTitle
    getAuthor // Exporta la función getAuthor
} */
